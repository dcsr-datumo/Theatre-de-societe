# Copy this file to dasch.env and keep the populated copy outside Git.
# Replace the endpoint only after DaSCH confirms the central SPARQL URL.
DASCH_SPARQL_URL=https://db.ls-prod-server.dasch.swiss/dsp-repo/sparql
DASCH_USER=admin
DASCH_PASSWORD=
