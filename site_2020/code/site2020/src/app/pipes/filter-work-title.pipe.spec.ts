import { FilterWorkTitlePipe } from './filter-work-title.pipe';

describe('FilterWorkTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWorkTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
