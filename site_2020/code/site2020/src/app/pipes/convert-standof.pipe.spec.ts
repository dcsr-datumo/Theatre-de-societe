import { ConvertStandofPipe } from './convert-standof.pipe';

describe('ConvertStandofPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStandofPipe();
    expect(pipe).toBeTruthy();
  });
});
